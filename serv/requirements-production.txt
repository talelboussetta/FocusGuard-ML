# FocusGuard Production Requirements (Render)
# MINIMAL - Only essential packages for fast deployment

# ============================================================================
# REST API Dependencies (Core)
# ============================================================================

# FastAPI - Modern, high-performance web framework
fastapi>=0.109.0

# ASGI Server - Production-grade async server
uvicorn[standard]>=0.27.0

# Database ORM & Drivers
sqlalchemy>=2.0.25              # SQL toolkit and ORM
asyncpg>=0.29.0                 # Async PostgreSQL driver

# Data Validation
pydantic>=2.5.0                 # Data validation using Python type hints
pydantic-settings>=2.1.0        # Settings management
email-validator>=2.1.0          # Email validation for Pydantic EmailStr

# Authentication & Security
python-jose[cryptography]>=3.3.0  # JWT token handling
passlib[bcrypt]>=1.7.4            # Password hashing with bcrypt
python-multipart>=0.0.6           # Form data parsing

# Rate Limiting
slowapi>=0.1.9                  # Rate limiting for FastAPI

# Utilities
python-dotenv>=1.0.0            # Environment variable management

# ============================================================================
# RAG & AI Dependencies (Required for Chatbot)
# ============================================================================

# Vector Store
qdrant-client>=1.7.0            # Qdrant vector database client

# Embeddings (Local model - free, no API key)
sentence-transformers>=2.3.0    # Local embeddings with HuggingFace models
numpy>=1.24.0                   # Required by sentence-transformers

# LLM Integration (Only what we actually use)
huggingface-hub>=0.20.0         # Hugging Face Hub client (for inference API)

# HTTP Client
aiohttp>=3.9.0                  # Async HTTP client for API calls

# Parsing
PyYAML>=6.0.0                   # YAML parsing for knowledge base frontmatter

# ============================================================================
# REMOVED from production (not used or optional):
# ============================================================================
# sentry-sdk[fastapi]>=1.40.0     # Error tracking - optional, adds deployment time
# pillow>=10.0.0                  # Image processing - not critical for core features
# openai>=1.10.0                  # Not using OpenAI (using local embeddings)
# tiktoken>=0.5.0                 # Only needed for OpenAI
# anthropic>=0.18.0               # Optional alternative - not used
# psycopg2-binary>=2.9.9          # Redundant - asyncpg handles PostgreSQL
